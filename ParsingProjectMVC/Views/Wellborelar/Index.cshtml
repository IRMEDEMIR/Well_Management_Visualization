@model IEnumerable<ParsingProjectMVC.Models.WellboreModel>

<div class="text-center">
    <h1 class="display-4">Wellborelar</h1>

    <div class="text-right mb-3">
        <form asp-action="Create" method="post" class="form-inline">
            <input type="text" name="wellboreAdi" class="form-control mr-2" placeholder="New Wellbore Adı" required />
            <button type="submit" class="btn btn-success">Add New Wellbore</button>
        </form>
    </div>

    <table class="table table-striped">
        <thead>
            <tr>
                <th>ID</th>
                <th>Wellbore Adı</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var wellbore in Model)
            {
                <tr>
                    <td>@wellbore.Id</td>
                    <td>@wellbore.WellboreAdi</td>
                    <td>
                        <a asp-action="Update" asp-route-id="@wellbore.Id" class="btn btn-primary btn-sm">Update</a>
                        <form asp-action="Delete" asp-controller="Wellborelar" method="post" style="display:inline;">
                            <input type="hidden" name="id" value="@wellbore.Id" />
                            <button type="submit" class="btn btn-danger btn-sm">X</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

<nav>
    <ul class="pagination">
        @for (int i = 1; i <= Math.Ceiling((double)ViewBag.TotalItems / ViewBag.PageSize); i++)
        {
            <li class="page-item @(ViewBag.PageNumber == i ? "active" : "")">
                <a class="page-link" href="/Wellborelar/Index?pageNumber=@i&pageSize=@ViewBag.PageSize">@i</a>
            </li>
        }
    </ul>
</nav>
